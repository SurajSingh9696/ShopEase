# ========================================
# SERVER CONFIGURATION
# ========================================
PORT=5000
NODE_ENV=production

# ========================================
# FRONTEND URL (CRITICAL FOR AUTHENTICATION)
# ========================================
# Your frontend deployed URL - MUST match exactly with your frontend domain
# Example: https://your-frontend-app.onrender.com
FRONTEND_URL=https://shopease-the-ecomm-site.onrender.com

# ========================================
# DATABASE
# ========================================
MONGODB_URL=mongodb+srv://username:password@cluster.mongodb.net/shopease?retryWrites=true&w=majority

# ========================================
# JWT SECRETS - Generate strong random strings
# ========================================
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
ACCESS_TOKEN_SECRET=your-access-token-secret-here-change-this-in-production
REFRESH_TOKEN_SECRET=your-refresh-token-secret-here-change-this-in-production

# ========================================
# COOKIE DOMAIN (Optional)
# ========================================
# Leave empty for same-domain cookies (recommended for most cases)
# Only set if cookies need to work across subdomains
# Example: .yourdomain.com (note the leading dot)
COOKIE_DOMAIN=

# ========================================
# RESEND EMAIL SERVICE
# ========================================
# Get API key from: https://resend.com/api-keys
RESEND_API_KEY=re_xxxxxxxxxxxxxxxxxxxx

# CRITICAL: Email sender MUST be from a verified domain in Resend
# You CANNOT use Gmail, Yahoo, or other third-party email providers
# 
# Options:
# 1. FOR TESTING: Use Resend's test domain
#    RESEND_FROM_EMAIL=ShopEase <onboarding@resend.dev>
#
# 2. FOR PRODUCTION: Use your own verified domain (REQUIRED)
#    - Go to: https://resend.com/domains
#    - Click "Add Domain" and add your domain
#    - Add the DNS records shown to your domain provider
#    - Wait for verification
#    - Use format: ShopEase <noreply@yourdomain.com>
RESEND_FROM_EMAIL=ShopEase <onboarding@resend.dev>
